# A_map_of_privacy-sensitivity_in_human_genomes-main

Requirements to visualize and run the scripts:
 Have python 3 installed
 Install jupyter lab if it is not allready install:
  Recomend (instaling) using anaconda navigator and instaling and running jupyter lab from there.
